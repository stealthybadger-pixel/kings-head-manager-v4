# Session 2026-07-16 — Blaze plan, Gemini model retirement, Invoice Scanner fixes

Continuation from yesterday (see `SESSION-2026-07-15.md` and `SESSION-2026-07-15-part4.md`).
Started on the work laptop reviewing what the home session (`68f7e8a` and earlier) had
shipped overnight, then moved to Invoice Scanner work. Everything below is committed and
pushed to `main`, auto-deployed live.

## Firebase billing — upgraded to Blaze
User had hit the Spark (free) plan's daily Firestore read quota again (64k reads — smaller
than a previous 500k incident, but still a hard block). Walked through upgrading to Blaze
(pay-as-you-go) directly in Firebase/Google Cloud Console (not something done via tooling —
needs a payment method, which is a user-only action). Also set up a **£5/month Google Cloud
Billing budget alert**, scoped to the `kings-head-kitchen-claude` project, as a safety net —
this was apparently already part of the user's Blaze setup flow. Confirmed for context: at
this app's actual read volume, even a 600k-read day would cost roughly $0.20 — Firestore
reads themselves are not a real billing risk here; the Spark plan's hard quota (not cost) was
the actual problem the read-optimisation work (yesterday's home session) was solving.

Also clarified a misconception: Firebase itself is not being deprecated. **Antigravity** is
Google's separate new agentic coding IDE, unrelated to Firestore/Hosting/Auth, which remain
fully active.

## Gemini API — dedicated account + model retirement bug (the big one)
User set up a **separate Google Cloud project** (`khkm-gemini-scanner`) specifically to hold
a Gemini API key for Invoice Scanner + Kitchen's recipe-scan feature, isolating that billing
from the main Firebase project. Both features already read the key from the same
`localStorage` key (`geminiApiKey`), entered via a Settings field on the Invoice Scanner
page — no code change was needed for the account-separation part itself, just generating a
new key at aistudio.google.com/apikey (the classic AI Studio flow, not the newer Vertex AI
"Agent Platform" console, which requires service-account binding for Gemini/Vertex keys and
was a dead end) and pasting it into that field.

**Root cause found while testing the new key**: both features were hardcoded to
`gemini-1.5-flash`, which has been deprecated/retired by Google — every scan attempt was
returning a 404. Confirmed via web search this model is gone; current recommended stable
model is `gemini-3.5-flash`. Fixed in both files (commit `1d00857`).

### Follow-up: gemini-3.5-flash itself intermittently 503ing
After the model-name fix, image-based scans (both a photo and a PDF) kept hitting a 503
("high demand") — but plain-text `curl` calls to the same model succeeded every time,
confirmed via direct testing. This narrowed it to `gemini-3.5-flash`'s **image-processing
path specifically** being under real capacity pressure on Google's side, not a code bug, not
a transient blip retries alone would fix, and not specific to any one file (both a PDF and an
iPhone photo failed identically).

Built out `src/utils/gemini.ts` as a shared client for both features (previously
near-duplicated inline fetch code in each), with:
- **Image compression** before upload — downscales to max 1600px longest side, re-encodes as
  JPEG at 0.85 quality. PDFs pass through untouched (canvas can't rasterize them, and Gemini
  accepts `application/pdf` natively). Cuts payload size/cost with no OCR accuracy loss.
- **Retry logic** for transient 503/429 errors (2 retries per model, backing off).
- **`thinkingLevel: 'low'`** — discovered via testing that `gemini-3.5-flash` spends hidden
  "thinking" tokens on every call (115 thought tokens just to say "Hello" in one test) which
  cost money on top of visible input/output tokens. Can't be fully disabled on this model,
  but `'low'` avoids paying for reasoning depth a straightforward extraction task doesn't need.
- **Model fallback chain** — tries `gemini-3.5-flash` first, falls back to `gemini-2.5-flash`
  (presumably less demand-contended, being an older model) if the primary keeps 503ing,
  rather than failing the scan outright.

Both Invoice Scanner and Kitchen's recipe scan now use this shared client. Also swapped both
features' pantry-matching from a naive substring check to the shared `findBestIngredientMatch`
fuzzy matcher (same one Catalog/Pantry use) — handles this app's "Item - Descriptor" naming
(e.g. "Dill" now correctly matches Pantry's "Dill - Fresh"). Runs against the already-cached
`ingredients` query (5-minute staleTime shared query client) — **no new Firestore reads**,
which mattered given the whole session started because of a read-quota problem.

## Invoice Scanner bugs found via real-world testing (a real Booker invoice photo)
- **Qty/price doubling**: the prompt was ambiguous between an invoice's per-unit PRICE column
  and its extended VALUE column (PRICE × QTY). A line like "Philadelphia, QTY 2, PRICE 16.99,
  VALUE 33.98" was extracting `packCost: 33.98` instead of `16.99` — since Pantry pricing is
  always per-single-pack, this silently doubled (or worse) any multi-quantity invoice line.
  Fixed by making the prompt explicit about PRICE vs. VALUE with a worked example.
- **No way to fix an unmatched line**: added a "Link to Pantry Item" control on unmatched
  lines — a dropdown of all ingredients, picking one recomputes the current-price/diff fields
  identically to an automatic match, so it slots into Price Changes / No Change correctly.
  Expected to be needed often until the user finishes manually linking all ~3,000 supplier
  catalogue products to their canonical Pantry names (see `wip/catalogue-matcher` branch,
  parked from yesterday, built exactly for that job).
- **Fixed a related latent bug** while touching `handleUpdatePrice`: it previously blindly
  overwrote whichever supplier was `isPreferred` (or index 0) regardless of whether that
  supplier's name actually matched the invoice's supplier — for a freshly manually-linked
  ingredient with zero existing suppliers, this silently did nothing (mapping over an empty
  array). Now matches by supplier name first, falls back to preferred/index-0 for the common
  case, and appends a brand-new supplier entry if neither exists.

## Follow-up: gemini-3.5-flash 429s and a 404 on the fallback (same session, later)
After the qty/price fix, user kept hitting a mix of 429/404/503 spot-checking real scans.
Diagnosed step by step:
- Direct `curl` calls to `gemini-3.5-flash` with a real image consistently returned the same
  Google-side 503 "high demand" message seen earlier — confirmed genuinely persistent, not
  transient, directly from Google's own error text.
- A 404 appeared on the `gemini-2.5-flash` fallback specifically. Pulled the full model list
  via `GET /v1beta/models?key=...` and confirmed `gemini-2.5-flash` **does** exist and support
  `generateContent` for this key — so the 404 wasn't a bad model name, more likely a strange
  side-effect of the whole project being hard-throttled (Google doesn't always surface a clean
  429 for every failure mode once a key is fully rate-limited).
- Root cause: `khkm-gemini-scanner` (the dedicated Gemini project set up this session) was
  never linked to billing — AI Studio was prompting to **buy prepaid credits** to unlock full
  usage, which the user (reasonably) declined rather than hand over payment details for what
  should be a near-free feature.
- Researched actual 2026 free-tier limits: `gemini-3.5-flash` (newest, most in-demand model)
  has a very tight free allowance, while **`gemini-2.5-flash-lite` gets 15 requests/min and
  1,000/day free** — comfortably >100x this app's real usage (avg 2/day, ~30/week peak).
- Fix (commit `a0efcb1`): reordered `GEMINI_MODELS` in `src/utils/gemini.ts` to lead with
  `gemini-2.5-flash-lite`, falling back to `gemini-2.5-flash` — no `gemini-3.5-flash` in the
  chain at all anymore. Stays entirely within the free tier, no credits/billing needed on this
  project. **User has not linked billing on `khkm-gemini-scanner` and does not want to** —
  respect that if picking this up again; the fix is to stay within free-tier limits, not to
  push billing on them a second time.

## Separate bug discovered, NOT yet investigated or fixed
While testing Invoice Scanner, the browser console showed **dozens of
`[Zod Schema Validation Failure]` errors** on real `ingredients` collection docs (e.g.
`ing_master_beans_buttertinned`, `ing_master_guinness`, `ing_master_ketchup`, and many more —
mostly older/legacy-looking items: tinned goods, spirits, spices, sauces). This is unrelated
to Gemini entirely. Depending on how `fetchCollection`'s Zod parsing handles a per-doc
validation failure (likely silently dropping the doc rather than crashing, given the app kept
working), **this could mean a meaningful number of real Pantry ingredients are being silently
excluded from every screen that calls `useIngredients()`** — including Invoice Scanner/recipe
scan matching, Catalog's cheaper-option detection, Stock, everywhere. Worth investigating
next session: find `fetchCollection` in `useKitchenData.ts`, see what it actually does on a
validation failure, and inspect one of the failing docs directly in the emulator/prod
Firestore to see which field(s) don't satisfy `IngredientSchema` (`src/types.ts`) — likely
some older docs predate a field that's now required, or have a value shape that changed.

## Deliberately not touched today
- **Price-sync orchestrator** (`scripts/reconcile_active_menu_prices.ts`, on hold since
  yesterday) — explicitly parked until the Firestore read-quota cleanup work is fully done.
  Still sitting locally/pushed but dev-only-gated, no further work this session.
- **`wip/catalogue-matcher` branch** — not touched, still parked pending review. Directly
  relevant to the "invoice matching will improve" comment above; worth picking up once
  Invoice Scanner work settles.

## Commits this session
`1d00857` (retired model fix), `c6c741c` (shared Gemini client + compression + fuzzy
matching), `74cde86` (thinkingLevel), `bfd2212` (model fallback), `c62b024` (qty/price
doubling fix + manual pantry linking), `a0efcb1` (switch to free-tier-friendly model order).
All on `origin/main`, all live.

## Suggested next steps
1. **Investigate the Zod validation failures** (see above) — potentially the highest-impact
   item, since it may be silently hiding real Pantry ingredients from matching/costing
   app-wide, not just in Invoice Scanner.
2. Spot-check the Invoice Scanner against a few more real invoices (different suppliers,
   different layouts) now that the qty/price bug is fixed — that bug likely affected every
   multi-quantity line scanned before today, worth double-checking nothing was already
   written back with a doubled price.
3. Confirm the `gemini-2.5-flash-lite` free-tier switch has actually resolved the 429/503/404
   mix in real use (not just in theory) — if it's still flaky, the next lever is requesting a
   manual quota increase on `khkm-gemini-scanner` (still free, just needs a Cloud Console
   request), not linking billing.
4. Consider picking `wip/catalogue-matcher` back up once satisfied with Invoice Scanner —
   the two features solve overlapping problems (getting supplier products correctly linked
   to canonical Pantry ingredients).
5. Price-sync orchestrator remains parked — revisit once confident the Firestore
   read-quota situation is genuinely settled (a few Blaze-plan days without hitting a
   quota wall would be a reasonable bar).
